package playground;
class Person{
    public static final int N_Eyes=2;
}
public class JavaPlayground {
    public static void main(String[] args) {
        System.out.println(Person.N_Eyes);
    }
}
//  val bufferedSource = io.Source.fromFile("imdb_movies.csv")
//  for (line <- bufferedSource.getLines) {
//    val cols = line.split(",").map(_.trim)
//    // do whatever you want with the columns here
//    println(s"${cols(0)}|${cols(1)}|${cols(2)}|${cols(3)}")
//  }
//  bufferedSource.close
//  for (line <- bufferedSource.getLines) {
//    val Array(month, revenue, expenses, profit) = line.split(",").map(_.trim)
//    println(s"$month $revenue $expenses $profit")
//  }




//
//    for(i <- 0 to line2.length-1) {
////      val imdb_title_id = line2(i)
////      val title = title
//       // print(line2(i))
//       //print(line2(0))
//      //print("   ")
//    }
//    print(line2(5))




